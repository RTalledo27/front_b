import { provideHttpClient } from '@angular/common/http';import { HttpTestingController,provideHttpClientTesting } from '@angular/common/http/testing';import { TestBed } from '@angular/core/testing';import { API_BASE_URL } from '../../../core/api/api.config';import { HttpGameEngineRepository } from './game-engine.repository';
describe('HttpGameEngineRepository',()=>{let repo:HttpGameEngineRepository;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting(),HttpGameEngineRepository,{provide:API_BASE_URL,useValue:'/api/v1'}]});repo=TestBed.inject(HttpGameEngineRepository);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('sends a unique draw command header',()=>{repo.draw('game-1','11111111-1111-4111-8111-111111111111').subscribe();const request=http.expectOne('/api/v1/admin/games/game-1/draws');expect(request.request.method).toBe('POST');expect(request.request.headers.get('X-Draw-Command-Id')).toBe('11111111-1111-4111-8111-111111111111');request.flush({data:{}});});it('requests the complete draw audit page',()=>{repo.draws('game-1').subscribe();http.expectOne('/api/v1/admin/games/game-1/draws?per_page=100').flush({data:[],links:{first:null,last:null,prev:null,next:null},meta:{current_page:1,from:null,last_page:1,links:[],path:'',per_page:100,to:null,total:0}});});});