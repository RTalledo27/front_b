import { provideHttpClient } from '@angular/common/http';import { HttpTestingController,provideHttpClientTesting } from '@angular/common/http/testing';import { TestBed } from '@angular/core/testing';import { API_BASE_URL } from '../../../core/api/api.config';import { CreateAdminGameDto } from '../models/admin-games.models';import { HttpAdminGamesRepository } from './admin-games.repository';
describe('HttpAdminGamesRepository',()=>{let repo:HttpAdminGamesRepository;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting(),HttpAdminGamesRepository,{provide:API_BASE_URL,useValue:'/api/v1'}]});repo=TestBed.inject(HttpAdminGamesRepository);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('creates games with the backend contract',()=>{const dto:CreateAdminGameDto={slug:'bingo-test',name:'Bingo test',description:null,number_min:1,number_max:90,hits_required:5,ticket_price_cents:500,prize_cents:100000,currency:'PEN',draw_interval_seconds:10,auto_draw_enabled:false,sales_opens_at:null,sales_closes_at:null,scheduled_start_at:null,settings:null};repo.create(dto).subscribe();const request=http.expectOne('/api/v1/admin/games');expect(request.request.method).toBe('POST');expect(request.request.body).toEqual(dto);request.flush({data:{}});});it('uses the game UUID for lifecycle and number endpoints',()=>{repo.transition('game id','open-sales').subscribe();const transition=http.expectOne('/api/v1/admin/games/game%20id/open-sales');expect(transition.request.method).toBe('POST');transition.flush({data:{}});repo.numbers('game id').subscribe();http.expectOne('/api/v1/admin/games/game%20id/numbers').flush({data:[]});});});