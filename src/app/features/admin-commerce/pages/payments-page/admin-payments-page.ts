import { ChangeDetectionStrategy, Component, inject } from '@angular/core';
import { RouterLink } from '@angular/router';
import { StatusBadge } from '../../../../shared/ui/status-badge/status-badge';
import { formatGameDate, formatMoney } from '../../../public-games/utils/public-game-display';
import { commerceStatusTone, formatShortId, paymentStatusLabel } from '../../../player-commerce/utils/player-commerce-display';
import { AdminPaymentsFacade } from '../../data-access/admin-commerce.facades';
@Component({selector:'app-admin-payments-page',imports:[RouterLink,StatusBadge],providers:[AdminPaymentsFacade],template:`<section class="page"><header class="page-header"><div><p class="eyebrow">Validación</p><h1>Pagos</h1><p>Revisa comprobantes y resuelve pagos pendientes con trazabilidad.</p></div></header><div class="filters" aria-label="Filtrar pagos">@for(o of filters;track o.value){<button type="button" [class.active]="facade.filter()===o.value" (click)="facade.load(1,o.value)">{{o.label}}</button>}</div>@if(facade.status()==='loading'){<section class="surface-card data-state" aria-busy="true"><span class="data-loader"></span><p>Cargando pagos…</p></section>}@else if(facade.status()==='error'){<section class="surface-card data-state" role="alert"><h2>No pudimos cargar los pagos</h2><p>{{facade.error()?.message}}</p><button class="button" (click)="facade.load()">Reintentar</button></section>}@else if(facade.status()==='empty'){<section class="surface-card data-state"><h2>Sin resultados</h2><p>No hay pagos para este estado.</p></section>}@else{<div class="list">@for(payment of facade.payments();track payment.id){<article class="surface-card row"><div><small>Pago {{shortId(payment.id)}}</small><h2>{{payment.order?.game?.name||'Orden '+shortId(payment.order_id)}}</h2><p>{{payment.method}} · enviado {{date(payment.submitted_at)}}</p></div><strong>{{money(payment.amount_cents,payment.currency)}}</strong><app-status-badge [tone]="tone(payment.status)">{{label(payment.status)}}</app-status-badge><a class="button button--secondary" [routerLink]="['/admin/pagos',payment.id]">Revisar</a></article>}</div><nav class="pagination" aria-label="Paginación"><button class="button button--secondary" [disabled]="!facade.hasPreviousPage()" (click)="facade.previousPage()">Anterior</button><span>Página {{facade.pageInfo().currentPage}} de {{facade.pageInfo().lastPage}}</span><button class="button button--secondary" [disabled]="!facade.hasNextPage()" (click)="facade.nextPage()">Siguiente</button></nav>}</section>`,styles:`.filters{display:flex;gap:.5rem;overflow:auto;margin-bottom:var(--s5)}.filters button{flex:none;padding:.6rem .8rem;border:1px solid var(--color-border);border-radius:999px;background:#fff;color:var(--color-text-muted);font:inherit;font-size:var(--sm);font-weight:700}.filters button.active{border-color:var(--color-brand);background:var(--color-brand-subtle);color:var(--color-brand)}.list{display:grid;gap:var(--s3)}.row{display:grid;grid-template-columns:1fr auto auto auto;align-items:center;gap:var(--s4);padding:var(--s5)}h2,p{margin:.15rem 0}.row small,.row p{color:var(--color-text-muted)}.pagination{display:flex;justify-content:center;align-items:center;gap:var(--s4);margin-top:var(--s5)}@media(max-width:48rem){.row{grid-template-columns:1fr auto}.row>a{grid-column:1/-1}.row .button{width:100%}.pagination{justify-content:space-between}.pagination span{font-size:var(--xs)}}`,changeDetection:ChangeDetectionStrategy.OnPush})
export class AdminPaymentsPage{readonly facade=inject(AdminPaymentsFacade);readonly money=formatMoney;readonly date=formatGameDate;readonly shortId=formatShortId;readonly label=paymentStatusLabel;readonly tone=commerceStatusTone;readonly filters=[{value:'',label:'Todos'},{value:'under_review',label:'En revisión'},{value:'approved',label:'Aprobados'},{value:'rejected',label:'Rechazados'},{value:'pending',label:'Pendientes'},{value:'refunded',label:'Reembolsados'}] as const;constructor(){this.facade.load();}}